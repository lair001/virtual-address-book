// establishing namespace for utils module

(function() {

	"use strict";

	if (typeof(modules.Utils) === 'undefined') {
		modules.Utils = {};

	} else {
		throw 'Fatal error: modules.PoeticRivals already exists.';
	}

})();